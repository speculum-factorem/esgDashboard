spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/esg-dashboard-test
    redis:
      host: localhost
      port: 6379
      database: 1

logging:
  level:
    com.esg.dashboard: DEBUG
    org.springframework.test: WARN

app:
  cache:
    ttl-minutes: 1
  features:
    real-time-updates: false